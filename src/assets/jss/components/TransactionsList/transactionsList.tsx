import { createStyles } from '@material-ui/core'

const transactionStyle = () =>
  createStyles({
    root: {
      display: 'flex',
      alignItems: 'center',
    },
  })
export default transactionStyle
