import { createStyles } from '@material-ui/core'

const splashStyle = () =>
  createStyles({
    root: {
      fontSize: '11px',
      display: 'flex',
      alignItems: 'center',
    },
  })

export default splashStyle
